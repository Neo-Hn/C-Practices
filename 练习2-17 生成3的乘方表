#include<stdio.h>
#include<math.h>

int main(void)
{
    int i, n;
    long power;
    
    scanf("%d", &n);
    
    for( i=0; i<=n; ++i){
        power = pow(3, i );
        printf("pow(3,%d) = %ld\n", i, power);
    }
    
    return 0;
}
